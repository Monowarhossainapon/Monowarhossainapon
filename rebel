# include <stdio.h>
int main ()
{
    int x,y,product=1,i=1;

    printf("Enter the value of x & y :\n");
    scanf("%d%d",&x,&y);

    while (i<=y){
        product *=x;
        i++;
    }
    printf("product =%d\n",product);

    return 1;//1 because x to the power zero equal 1
}
